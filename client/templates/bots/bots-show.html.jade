template(name='botsShow')
  .card-panel
    span.card-title
      h2 {{name}}
    {{> botRuler }}
    .row
      .col.s10
        if isScriptGone 
          p.red-text.darken-4 The associated script has been removed.
        else
          if readme
            {{{ readme }}}    
          else
            |Readme missing
