template(name='botsShow')
  .card-panel
    {{> botRuler }}
    span.card-title
      h2 {{name}}
    .row
      .col.s10
        if isScriptGone 
          p.orange-text.darken-4 The associated script has been removed.
        else
          if readme
            {{{ readme }}}    
          else
            |Readme missing
